{/*
  Troubleshooting Guide | 故障排除指南
  
  本文档记录常见问题的应急处理方案和修复脚本
  This document records emergency solutions and fix scripts for common issues
*/}

# Troubleshooting Guide | 故障排除指南

## 概览 Overview

本文档提供 MediCareAI 系统的常见故障排除指南和应急脚本说明。

This document provides troubleshooting guides and emergency scripts for MediCareAI system.

---

## 🆘 应急脚本 Emergency Scripts

### 1. Docker 清理脚本 Docker Cleanup Script

**文件位置 | Location**: `scripts/cleanup-docker.sh`

**用途 | Purpose**:
清理所有 Docker 数据，包括容器、卷、网络。用于完全重置环境。

Clean all Docker data including containers, volumes, networks. Used for complete environment reset.

**使用场景 | When to Use**:
- 全新部署前清理环境
- Docker 状态异常需要重置
- 磁盘空间不足需要清理

**使用方法 | Usage**:
```bash
# 交互模式（默认）
./scripts/cleanup-docker.sh

# 自动确认模式（非交互式）
./scripts/cleanup-docker.sh -y
# 或
./scripts/cleanup-docker.sh --yes
```

**⚠️ 警告 | Warning**:
此脚本会删除所有数据，包括数据库！使用前务必备份。

This script deletes ALL data including databases! Backup before use.

---

## 🔧 常见问题 Common Issues

### 问题 1: PostgreSQL 健康检查失败

**现象 | Symptoms**:
```
medicare_postgres is unhealthy
```

**解决方案 | Solution**:
已在 `docker-compose.yml` 中添加 `start_period: 60s` 给数据库足够的初始化时间。

Already added `start_period: 60s` in `docker-compose.yml` for database initialization.

---

### 问题 2: Docker Compose 命令不兼容

**现象 | Symptoms**:
```
docker-compose: command not found
```

**解决方案 | Solution**:
`scripts/cleanup-docker.sh` 已兼容 Docker Compose v1 (`docker-compose`) 和 v2 (`docker compose`)。

The cleanup script supports both Docker Compose v1 and v2.

---

### 问题 3: 环境变量未同步到容器

**现象 | Symptoms**:
修改 `.env` 文件后，容器内的配置未更新。

**解决方案 | Solution**:
```bash
# 重启后端服务
docker-compose restart backend

# 或者完全重新创建容器
docker-compose stop backend
docker-compose rm backend
docker-compose up -d backend
```

**原理 | Explanation**:
Docker 的卷挂载可能需要重启容器才能同步最新的 `.env` 文件内容。

---

### 问题 4: 首次部署数据库连接失败

**现象 | Symptoms**:
```
Connection refused to PostgreSQL
```

**解决方案 | Solution**:
等待数据库完全启动：
```bash
# 检查 PostgreSQL 健康状态
docker-compose ps

# 查看日志
docker-compose logs postgres

# 等待健康检查通过后重启后端
docker-compose restart backend
```
---

### 问题 5: API_KEY_MASTER_KEY 环境变量错误

**现象 | Symptoms**:
```
ValueError: API_KEY_MASTER_KEY environment variable is required
```
或后端启动时提示加密密钥错误。

**原因 | Cause**:
从 v2.1.2 版本开始，系统移除了硬编码的加密密钥，必须通过环境变量 `API_KEY_MASTER_KEY` 配置。

**解决方案 | Solution**:

1. 生成安全的随机密钥:
```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

2. 编辑 `.env` 文件，添加生成的密钥:
```bash
API_KEY_MASTER_KEY=your_generated_key_here
```

3. 重新部署后端服务:
```bash
docker-compose stop backend
docker-compose rm backend
docker-compose up -d backend
```

**警告 | Warning**:
- 一旦设置 `API_KEY_MASTER_KEY`，请勿更改！
- 更改此密钥会导致数据库中已加密的 API 密钥无法解密
- 请在首次部署前妥善保存此密钥

---
---

## 📝 维护任务 Maintenance Tasks

### 备份数据库 Backup Database

```bash
# 使用内置备份脚本
./scripts/backup.sh

# 手动备份
docker exec medicare_postgres pg_dump -U medicare_user medicareai > backup_$(date +%Y%m%d).sql
```

### 查看日志 View Logs

```bash
# 所有服务日志
docker-compose logs

# 特定服务
docker-compose logs backend
docker-compose logs postgres
docker-compose logs redis

# 实时跟踪
docker-compose logs -f backend
```

### 重启服务 Restart Services

```bash
# 重启所有服务
docker-compose restart

# 重启特定服务
docker-compose restart backend
docker-compose restart postgres
```

---

## 🐛 调试技巧 Debug Tips

### 检查容器状态 Check Container Status

```bash
docker-compose ps
docker stats
```

### 进入容器调试 Enter Container for Debug

```bash
# 进入后端容器
docker exec -it medicare_backend /bin/sh

# 进入数据库容器
docker exec -it medicare_postgres psql -U medicare_user -d medicareai
```

### 测试 API Test API

```bash
# 健康检查
curl http://localhost/health

# 测试登录
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@medicare.ai","password":"admin123456"}'
```

---

## 📊 系统资源监控 System Resource Monitoring

### 查看容器资源使用 Container Resource Usage

```bash
docker stats
```

### 查看磁盘使用 Disk Usage

```bash
docker system df
docker system df -v
```

### 清理未使用资源 Cleanup Unused Resources

```bash
# 清理未使用的镜像
docker image prune

# 清理未使用的卷
docker volume prune

# 清理所有未使用资源
# 警告：这会删除所有停止的容器、未使用的网络、镜像和构建缓存
docker system prune -a
```

---

## 🔐 安全配置 Security Configuration

### SELinux 问题 SELinux Issues

如果使用 SELinux 的系统（如 Fedora、RHEL、CentOS）遇到权限问题，请参考：

For SELinux-enabled systems (Fedora, RHEL, CentOS), see:

**📄 [SELINUX-GUIDE.mdx](SELINUX-GUIDE.mdx)**

---

## 📞 获取帮助 Get Help

### 文档资源 Documentation Resources

- **[README.md](../README.md)** - 项目概览和快速开始
- **[DEPLOYMENT.mdx](DEPLOYMENT.mdx)** - 详细部署指南
- **[CHANGELOG.md](../CHANGELOG.md)** - 版本历史记录

### 报告问题 Report Issues

如果遇到未在此文档中列出的问题：

1. 查看服务日志：`docker-compose logs [service]`
2. 检查 [CHANGELOG.md](../CHANGELOG.md) 了解已知问题
3. 在 GitHub Issues 中搜索类似问题
4. 创建新的 Issue 并提供详细的错误信息和日志

---

**最后更新 | Last Updated**: 2026-02-16  
**版本 | Version**: v2.0.3  
**维护者 | Maintainer**: 苏业钦 (Su Yeqin)
